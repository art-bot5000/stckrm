# Dependencies
node_modules/
package-lock.json

# Built output — generated by CI, not committed to git
public/

# Fly.io
.fly/

# Environment / secrets — NEVER commit these
.env
.env.local
*.env

# OS
.DS_Store
Thumbs.db
